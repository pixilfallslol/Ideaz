let windowWidth = 1920
let windowHeight = 1080

function preload() {
}

function setup() {
  let CANVAS = createCanvas(windowWidth, windowHeight)
  CANVAS.position(0,0)
}

function draw(){
  background(255)
}